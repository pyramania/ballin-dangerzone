java:
  java_version: java-7-openjdk
  maven_version: '3.0.5'  # 2.0, 3.0, 3.1, and 3.2
tests:
  - 'echo $HOME'
profiles:
  default:
    tests:
      - command: mvn test
        type: junit
        report_files:
        - "target/surefire-reports/*.xml"
