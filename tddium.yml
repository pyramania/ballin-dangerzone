tddium:
  tests:
  - "ruby -v"
  profile:
    test_profile:
      SPLIT:golang_versions:
        golang_version: "1.1.2"
        ruby_version: "ruby-2.1.2"
  plan:
    - "test_profile"
    - "default"
