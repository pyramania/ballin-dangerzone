plan:
  - default
  - integration
profiles:
  default:
    tests:
     - type: custom
       command: sleep 10
       invocation: single
       output: exit-status
     - type: custom
       command: sleep 15
       invocation: single
       output: exit-status
    test_pattern:
     - default/**
  integration:
    tests:
     - type: custom
       command: sleep 11
       invocation: single
       output: exit-status
     - type: custom
       command: sleep 14
       invocation: single
       output: exit-status
    test_pattern:
     - integration/**
rerun_list:
  - integration/**
