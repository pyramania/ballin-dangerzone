ruby_version: ruby-1.9.2-p320
bundler_version: 1.7.6
plan:
  - default
  - integration
profiles:
  default:
    tests:
     - type: custom
       command: sleep 10
       invocation: single
       output: exit-status
     - type: custom
       command: sleep 15
       invocation: single
       output: exit-status
    test_pattern:
     - default/**.sh
  integration:
    tests:
     - type: custom
       command: sleep 11
       invocation: single
       output: exit-status
     - type: custom
       command: sleep 14
       invocation: single
       output: exit-status
    test_pattern:
     - integration/**.sh
rerun_list:
  - integration/one.sh
